import {logo} from "../static/img/mlecole.svg"

const Navbar = () => {
  return (
      <nav>
        // Logo
        <img src={logo} />
        <h1>MLEcole</h1>
        // Right hand side
      </nav>
  )
}

export default Navbar;